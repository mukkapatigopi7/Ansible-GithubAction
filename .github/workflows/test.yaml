name: test github action
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
jobs:
  hello:
   runs-on: ubuntu-latest
   steps:
     - uses: actions/checkout@v2
     - name: hello world
       run: echo "hello world"
       shell: bash
  bye:
   runs-on: ubuntu-latest
   steps:
     - uses: actions/checkout@v2
     - name: good bye
       run: echo "good bye"
       shell: bash
